# FE (frontend) — not started yet

Planned: a React web client replacing the JavaFX desktop UI in
[`../legacy`](../legacy), so the app can be deployed and used from a
browser instead of run as a local desktop app.

Nothing here yet — the JavaFX UI code (`Main.java`, `AvatarCatalog.java`,
`ChatTheme.java`) stays in `legacy/` since it doesn't translate directly to
web code; it's a reference for the features/flows to reimplement, not code
to port as-is.
